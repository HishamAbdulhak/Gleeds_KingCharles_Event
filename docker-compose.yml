services:
  db:
    image: postgres:18
    environment:
      POSTGRES_DB: quiz
      POSTGRES_USER: quiz
      POSTGRES_PASSWORD: quiz
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql   # 18+ keeps its data under here, in 18/docker

volumes:
  pgdata:
