spring:
  application:
    name: quiz
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/quiz}
    username: ${DB_USER:quiz}
    password: ${DB_PASSWORD:quiz}
  jpa:
    hibernate:
      ddl-auto: validate   # Flyway owns the schema
    open-in-view: false
